services:

  backend:
    build: ./backend
    container_name: ai-interview-backend
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: ai-interview-frontend
    ports:
      - "3000:80"
    restart: unless-stopped